<?php

if(password_verify($_POST['password'], $hash)) {
	$_SESSION['loggedin'] = true;
	$_SESSION['name'] = $row['Name'];
	$_SESSION['start'] = time();
	$_SESSION['expire'] = $_SESSION['start'] + (1 * 60);

	echo "<div class='alert alert-success' role='alert'><strong>Welcome!</strong> $row[Name]
	<p><a href='edit-profile.php'>Edit Profile</a></p>
	<p><a href='logout.php'>Logout</a></p></div>";
} else {
	echo "<div class='alert-danger' role='alert'>Email or Password are incorrect!
	<p><a href='login.php'><strong>Please try again!</strong></a></p></div>";
}

if (isset($_SESSION['loggedin']) && $_SESSION['loggedin'] == true){
	//SESION INICIADA
}else{
	echo "<div class='alert alert-danger' role='alert'>
	<h4>Your session has expired!</h4>
	<p><a href='login.html'>Login Here</a></p></div>";
	exit;
}

?>
